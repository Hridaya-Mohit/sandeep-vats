import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds — deep cosmic indigo, not pure black
  static const Color background  = Color(0xFF0F0C1D); // deep indigo-black
  static const Color surface     = Color(0xFF17132C); // indigo surface
  static const Color surfaceLight = Color(0xFF201C3A); // card / lifted surface

  // Gold palette
  static const Color gold      = Color(0xFFC9A84C);
  static const Color goldMuted = Color(0xFF9B7A40);
  static const Color goldGlow  = Color(0xFFD4A843);
  static const Color goldLight = Color(0xFFE8C97E);

  // Text
  static const Color textPrimary = Color(0xFFF2EDD8);
  static const Color textMuted   = Color(0xFFAA9E8C);
  static const Color textSubtle  = Color(0xFF7A6E62);

  // Borders
  static const Color border     = Color(0xFF2E2820);
  static const Color borderGold = Color(0xFF5A4820);
}
